apiVersion: "kubero.dev/v1alpha1"
kind: Pipeline
metadata:
  name: my-new-pipeline-example
  labels: 
    manager: 'kubero'
spec:
  name: my-awesome-app
  reviewapps: true
  phases:
    - name: test
      enabled: true
    - name: stage
      enabled: false
    - name: production
      enabled: false